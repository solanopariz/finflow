# Banco de dados (MySQL via Docker Compose)
DATABASE_URL="mysql://finflow:finflow@localhost:3306/finflow"

# Segredos JWT (gere valores aleatórios fortes em produção)
JWT_ACCESS_SECRET="troque-este-segredo"
JWT_REFRESH_SECRET="troque-este-outro-segredo"

# Anthropic Claude API (categorização e resumo por IA)
ANTHROPIC_API_KEY=""

# Servidor
PORT=4000
CLIENT_ORIGIN="http://localhost:5173"
